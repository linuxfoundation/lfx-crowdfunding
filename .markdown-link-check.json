{
  "ignorePatterns": [
    {
      "pattern": "^https://github.com/linuxfoundation/",
      "comment": "Private LFX repos return 404 to unauthenticated link checkers"
    },
    {
      "pattern": "^https://nuxt.com/modules",
      "comment": "nuxt.com/modules times out in CI (Status: 0) but is a valid page"
    },
    {
      "pattern": "^(?!https?://)",
      "comment": "Relative file links resolve correctly locally but return 400 in the MegaLinter container where the repo root is not served over HTTP"
    }
  ]
}
